{{ form_start(form) }}
    {{ form_row(form.titre) }}
    {{ form_row(form.dateSortie) }}
    {{ form_label(form.prix) }}
    {{ form_widget(form.prix) }} €
    {{ form_help(form.prix) }}
    {{ form_row(form.description) }}
    {{ form_row(form.imageUrl) }}

    {{ form_row(form.editeur) }}
    {{ form_row(form.genre) }}
    {{ form_row(form.developpeur) }}
    <button class="btn btn-dark btn-lg w-100 rounded-pill fw-bold shadow-sm py-2">
        <i class="bi bi-save-fill text-warning me-2"></i>{{ button_label|default('Sauvegarder') }}
    </button>
{{ form_end(form) }}
